import { TG_BlogPost } from "@/lib/shared/types";
import React from "react";

type Props = {
  blogPost: TG_BlogPost;
};

const BlogGridRow = ({ blogPost }: Props) => {
  return <div>BlogGridRow</div>;
};

export default BlogGridRow;
